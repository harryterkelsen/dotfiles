This is my dotfiles repo. It contains my configuration files for development.
It uses stow to put the dotfiles in the right place on the local machine.

## Git workflow

git add and git commit after each significant step. Make sure the commit
contains reasonable/cohesive set of changes, and don't include other edits.
Split the commit up into logical chunks if possible.
